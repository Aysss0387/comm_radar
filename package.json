{
  "name": "communication-paper-radar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run --with-requirements requirements.txt python -m comm_paper_radar.research_web --host 0.0.0.0 --port ${DEV_PORT:-3000}",
    "start": "uv run --with-requirements requirements.txt python -m comm_paper_radar.research_web --host 0.0.0.0 --port ${PORT:-3000}",
    "test": "uv run --with-requirements requirements.txt pytest -q"
  }
}
